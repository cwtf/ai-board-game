---
import ChessGame from '@/components/games/chess/ChessGame.svelte';
import Header from '@/components/shell/Header.svelte';
import SEO from '@/components/shell/SEO.astro';
import { absoluteSiteUrl, siteUrl } from '@/lib/site';
import '../styles/global.css';

const baseUrl = siteUrl();
const pageDescription =
  'Play 3D chess against configurable AI opponents in a browser-based local-first board game table.';
const pageImage = '/screenshots/splendor.png';
const chessJsonLd = {
  '@type': 'VideoGame',
  '@id': `${absoluteSiteUrl('/chess', baseUrl)}#game`,
  name: 'Chess AI Game',
  url: absoluteSiteUrl('/chess', baseUrl),
  description: pageDescription,
  image: absoluteSiteUrl(pageImage, baseUrl),
  gamePlatform: 'Web browser',
  applicationCategory: 'Game',
  operatingSystem: 'Web',
  numberOfPlayers: {
    '@type': 'QuantitativeValue',
    minValue: 2,
    maxValue: 2,
  },
  offers: {
    '@type': 'Offer',
    price: '0',
    priceCurrency: 'USD',
  },
};
---

<html lang="en">
  <head>
    <SEO
      title="Chess AI Game | AI Board Game Platform"
      description={pageDescription}
      path="/chess"
      image={pageImage}
      imageAlt="3D chess AI game table"
      jsonLd={chessJsonLd}
    />
  </head>
  <body class="flex h-screen flex-col overflow-hidden bg-neutral-950 text-neutral-100">
    <Header client:load backHref="/" />
    <main class="min-h-0 flex-1 overflow-hidden">
      <ChessGame client:load />
    </main>
  </body>
</html>
