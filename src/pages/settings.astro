---
import SettingsPanel from '@/components/ai/SettingsPanel.svelte';
import Header from '@/components/shell/Header.svelte';
import SEO from '@/components/shell/SEO.astro';
import { settingsSeo } from '@/lib/seo';
import '../styles/global.css';
---

<html lang="en">
  <head>
    <SEO
      title={settingsSeo.title}
      description={settingsSeo.description}
      path={settingsSeo.path}
      image={settingsSeo.image}
      imageAlt={settingsSeo.imageAlt}
      noindex
    />
  </head>
  <body class="min-h-screen bg-neutral-950 text-neutral-100">
    <Header client:load backHref="/" />
    <main>
      <SettingsPanel client:load />
    </main>
  </body>
</html>
